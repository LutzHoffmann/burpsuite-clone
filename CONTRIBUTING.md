# Contributing

Run `go test ./...` before submitting backend changes. Frontend changes must pass `npm test` and `npm run build` from `web/`.

Update documentation when setup, proxy behavior, or user-facing workflows change. Include screenshots with user interface changes and remove any sensitive data before sharing them.

Keep captured traffic, local databases, certificates, CA private keys, and secrets out of commits. Do not upload these materials to external services.
